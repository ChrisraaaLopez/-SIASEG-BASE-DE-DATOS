CALL p_asistencias_crud(
    'UPDATE',
    1,           -- id_asistencia a modificar
    NULL,        -- empleado_id (no cambia)
    NULL,        -- turno_id (no cambia)
    NULL,        -- estacion_id (no cambia)
    NULL,        -- zona_id (no cambia)
    'Tarde',     -- nuevo status
    'Llegó 10 minutos tarde'
);
